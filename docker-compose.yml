services:
  postgres:
    image: postgres:17-alpine
    container_name: qmarket-postgres
    environment:
      POSTGRES_DB: qmarket
      POSTGRES_USER: qmarket
      POSTGRES_PASSWORD: qmarket
    ports:
      - "5432:5432"
    volumes:
      - qmarket_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qmarket -d qmarket"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  qmarket_pg_data:

  # Uncomment to run API in Docker (build from repo root).
  # JWT_SECRET is required — generate e.g. `openssl rand -base64 48` and pass via env.
  # Do not commit real secrets.
  # api:
  #   build:
  #     context: .
  #     dockerfile: server/Dockerfile
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     SPRING_PROFILES_ACTIVE: prod
  #     QMARKET_SEED_ENABLED: "false"
  #     SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/qmarket
  #     SPRING_DATASOURCE_USERNAME: qmarket
  #     SPRING_DATASOURCE_PASSWORD: qmarket
  #     JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in the environment}
  #     DB_USER: qmarket
  #     DB_PASSWORD: qmarket
  #     CORS_ORIGINS: https://your-frontend.example
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
